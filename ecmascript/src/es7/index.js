let numbers = [1,2,3,7,8];

// nueva función includes (para saber si existe un valor dentro de un array)

if (numbers.includes(7)) {
    console.log("Si está el 7");
} else {
    console.log("No está");
}

// elevar un número a un potencia

let base = 4; 
let exponent = 2;
let result = base ** exponent;

console.log(result);

